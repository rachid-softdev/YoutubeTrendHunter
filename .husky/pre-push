# Graduated pre-push hook — non-blocking warnings only.
# Strict enforcement happens in CI. Never blocks a push.

pnpm --filter @youtube-trendhunter/web typecheck 2>/dev/null || echo "[pre-push] Web typecheck warnings (non-blocking)"
pnpm --filter @youtube-trendhunter/web test 2>/dev/null || echo "[pre-push] Web tests warnings (non-blocking)"
