name: Mobile CI

on:
  push:
    branches: [main]
    paths:
      - 'youtube-trendhunter-mobile/**'
      - 'packages/**'
  pull_request:
    branches: [main]
    paths:
      - 'youtube-trendhunter-mobile/**'
      - 'packages/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci-mobile:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build